// Lecture_Week1.cpp 

// Console Applications
// - Console applications are computer programs designed to be run in a text-based command lined interface (CLI) enviroment, aka Console

// cout = charatcer output
// cin = character input
#include <iostream>
#include <string>

void blockOfChar(char ch, int rows, int cols)
{
    for (int i = 0; i < rows; i++)
    {
        for (int j = 0; j < cols; j++)
        {
            std::cout << ch;
        }
        std::cout << "\n";
    }
}

int main()
{
    double average(std::count >> "Kyle";
}

// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu
